{
  "name": "llmkey",
  "version": "1.0.0",
  "description": "Manage LLM provider API keys as system environment variables",
  "type": "commonjs",
  "bin": {
    "llmkey": "./dist/index.js"
  },
  "scripts": {
    "build": "ncc build bin/llmkey.js -o dist",
    "build:binaries": "npm run build && pkg dist/index.js --targets node18-macos-arm64,node18-macos-x64,node18-linux-x64,node18-win-x64 --out-path binaries"
  },
  "dependencies": {
    "commander": "^11.0.0",
    "inquirer": "^8.2.6"
  },
  "engines": {
    "node": ">=16"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4",
    "pkg": "^5.8.1"
  }
}
